const { response } = require(express);

const cake = require("../models/cake");

module.exports = {
  index(resquest, response){
      try{
        const cakes = cake 
      }
      catch{

      }
  }
}